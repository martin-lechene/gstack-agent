# Changelog

## [1.0.0] - 2026-05-04

### Added
- Product-ready `gstack-agent` repository with branch adaptation support for `claude`, `cursor`, and `opencode`.
- Documentation and adaptation guidance expanded for additional runtime targets: `codex`, `antigravity`, `vscode`, `copilot`, and `phpstorm`.
- Cross-platform installation helpers: `install.sh` and `install.bat`.
- Validation workflow to prevent regression across all target branches.
- GitHub issue templates, pull request template, and contribution guidance.
- Versioning system with `VERSION` file and release-ready tagging guidance.
